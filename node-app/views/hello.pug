extends layout

block content
  p Hello
  //above will overwrite the default on layout.pug
block footer
  p I love you!!!
  //above will overwrite the default on layout.pug
  h2 Sale ends #{h.moment().endOf('day').fromNow()}



